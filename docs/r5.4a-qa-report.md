# R5.4a — Manual QA, Bug Bash & UX Review

**Fecha:** 2026-05-31  
**Alcance:** Validación flujo Grower R5.4 — sin features nuevas, sin migraciones, sin R5.5.

---

## FASE 1 — Build & toolchain

| Comando | Resultado | Notas |
|---------|-----------|-------|
| `supabase db reset` | **WARN** | Migraciones + seeds OK; `502` al reiniciar contenedores (Supabase sigue operativo en `:54321`) |
| `npm run db:types` | **PASS** | Types regenerados |
| `npm run build` | **PASS** | Rutas `/admin/dittobots`, `/profile/dittobots`, `/recetas` presentes |
| `npm test -- --run` | **PASS** | 237/237 |
| `npm run lint` | **PASS** (warnings preexistentes) | Tras fix — ver bugs |

### SyntaxError JSON — resuelto

**Causa:** `package-lock.json` corrupto en `node_modules/object.groupby` — clave `"engines"` sin `{` (posición ~357199).

**Fix aplicado:** restaurar bloque `"engines": { "node": ">= 0.4" }`.

**Verificación:** `JSON.parse(package-lock.json)` OK; `next lint` ya no emite SyntaxError.

---

## FASE 2 — Checklist manual 1–17

Leyenda: **PASS** verificado | **FAIL** defecto | **NOT TESTED** requiere sesión browser manual

| # | Check | Resultado | Evidencia |
|---|-------|-----------|-----------|
| 1 | Super Admin registra unidad en `/admin/dittobots` | **NOT TESTED** | Ruta + panel existen; requiere login super-admin en browser |
| 2 | Usuario autenticado ve "Mis DittoBots" en menú | **PASS** (código) | [`UserMenu.tsx`](../src/shared/shell/navbar/right/UserMenu.tsx) L219 |
| 3 | `/profile/dittobots` empty state (CTA + Activar) | **PASS** (código) | [`mis-dittobots-panel.tsx`](../src/domains/dittobots/presentation/mis-dittobots-panel.tsx) L108–173 |
| 4 | Activación serial + código válidos | **NOT TESTED** | Policy tests PASS; E2E browser pendiente |
| 5 | Tras activación, menú muestra "Recetas" | **NOT TESTED** | `/api/grower-status` + `isGrower` en UserMenu; requiere activación real |
| 6 | `/recetas` sin bot → redirect `/profile/dittobots` | **PASS** (código) | [`recetas/page.tsx`](../src/app/recetas/page.tsx) L27–28 |
| 7 | `/recetas` con bot → lista propios + biblioteca | **NOT TESTED** | Query + client existen; requiere datos seed + grower |
| 8 | Crear protocolo borrador | **NOT TESTED** | `createRecipeProtocolAction` implementado |
| 9 | Link producto al crear (`uses`) | **NOT TESTED** | Requiere UUID producto válido en entorno local |
| 10 | Calificar protocolo biblioteca | **NOT TESTED** | `rateRecipeProtocolAction` + `publication_review` |
| 11 | Super Admin ve serial/location/owner/visibility | **PASS** (código) | [`DittoBotInventoryPanel.tsx`](../src/shared/admin-ui/dittobots/DittoBotInventoryPanel.tsx) tabla admin |
| 12 | `npm run build` | **PASS** | Ejecutado OK |
| 13 | `npm test -- --run` | **PASS** | 237/237 |
| 14 | Activación hereda ubicación usuario (snapshot) | **PASS** (unit + SQL) | Policy test + SQL `location_region='Granja'` tras activate |
| 15 | Modificar ubicación dispositivo persiste | **NOT TESTED** | `updateDittoBotDeviceSettingsAction` + inline UI; browser pendiente |
| 16 | `is_public_on_map` persiste | **PASS** (SQL) | UPDATE verificado en DB |
| 17 | Super Admin ve ubicación/owner/estado | **PASS** (código) | Columnas admin; page guard `requireSuperAdmin()` |

### Policy tests (soporte checks 4–6, 9–10 parcial)

- `activate-ditto-bot.policy.test.ts` — código inválido, ya activado, snapshot location: **PASS**
- `device-location.policy.test.ts` — filterPublicMapDevices, inheritance: **PASS**

---

## FASE 3 — UX Review

### Navegación

| Área | Evaluación | Notas |
|------|------------|-------|
| Menú usuario | Bueno | "Mis DittoBots" siempre; "Recetas" condicional vía `/api/grower-status` |
| Flash grower | Mejorable | Menú carga auth → seller → grower secuencial; posible flash sin "Recetas" |
| Admin nav | Aceptable | Label inglés "DittoBot Inventory" mezclado con nav ES |
| Rutas | OK | `PROFILE_DITTOBOTS_PATH`, `RECETAS_PATH`, `ADMIN_DITTOBOT_INVENTORY_PATH` centralizadas |

### Mensajes de error

| Flujo | Mensaje | OK |
|-------|---------|-----|
| Serial inexistente | "No se encontró un dispositivo con ese número de serie." | Sí |
| Código incorrecto | "El código de activación no es válido." | Sí |
| Ya activado | "Este dispositivo ya fue activado." | Sí |
| Sin sesión | "Debés iniciar sesión para activar un DittoBot." | Sí |
| Errores inline | `role="alert"` + `text-destructive` | Sí |

### Estados vacíos

| Pantalla | Estado | CTA |
|----------|--------|-----|
| Mis DittoBots (0 devices) | Explicación clara | Comprar → `/contacto`, form Activar |
| Recetas (sin borradores) | Depende de datos | Form crear protocolo si `canCreate` |

### Textos / terminología

| Issue | Severidad |
|-------|-----------|
| UI dice "Recetas"; dominio = Protocolos | P1 — coherencia producto |
| "Configuración mínima" en device settings | OK para MVP |
| Admin descripción en español, título nav en inglés | P2 |

### CTA

- **Comprar DittoBot** → `CONTACT_PATH` (placeholder correcto para MVP)
- **Activar** — formulario visible en empty state y con devices
- **Guardar** settings — inline por dispositivo

---

## FASE 4 — Bugs encontrados

| ID | Severidad | Descripción |
|----|-----------|-------------|
| BUG-1 | **P0 (fixed)** | `package-lock.json` corrupto → `next lint` SyntaxError |
| BUG-2 | **P1** | `supabase db reset` termina con HTTP 502 al reiniciar contenedores (intermitente; DB usable) |
| BUG-3 | **P1** | Checklist browser 1–10, 15 sin validación E2E en esta sesión |
| BUG-4 | **P2** | UserMenu: posible flash antes de resolver `isGrower` |
| BUG-5 | **P2** | Admin nav item reutiliza `CATEGORIES_MANAGE` permiso (funciona hoy solo super-admin, naming confuso) |
| BUG-6 | **P2** | Device settings: solo `location_region` editable en UI; lat/lng no expuestos (schema sí los soporta) |
| BUG-7 | **P2** | Terminología Recetas vs Protocolos inconsistente con registry |

---

## P0 / P1 / P2 consolidado

### P0

- ~~package-lock.json corrupto~~ **RESUELTO en R5.4a**

### P1

- Completar QA browser manual checks 1, 4, 5, 7–10, 15 con cuentas super-admin + grower
- Investigar `502` post-reset Supabase CLI (upgrade CLI v2.102 recomendado)
- Alinear copy UI: "Recetas" vs "Protocolos" según decisión producto

### P2

- Reducir flash grower en UserMenu (server-side grower flag o skeleton)
- Renombrar admin nav / permiso dedicado `DITTOBOTS_MANAGE`
- Exponer lat/lng en settings o documentar solo región en MVP
- Capturas de pantalla para checklist (pendiente sesión manual)

---

## Capturas relevantes

No generadas en esta sesión (QA automatizada + code review).  
**Recomendación:** capturar en sesión manual:

1. `/admin/dittobots` — tabla con unidad registrada  
2. `/profile/dittobots` — empty state + post-activación  
3. UserMenu — sin/con "Recetas"  
4. `/recetas` — biblioteca + crear borrador  
5. Error activación — serial/código incorrecto  

---

## Veredicto — ¿Listo para R5.5?

| Criterio | Estado |
|----------|--------|
| Build + tests | **PASS** |
| Schema + migraciones | **PASS** (con WARN reset 502) |
| Flujo Grower E2E browser | **INCOMPLETO** — ~10/17 checks NOT TESTED |
| Bugs bloqueantes | **0** (post fix package-lock) |

### Recomendación

**Condicionalmente listo para R5.5.**

- **Técnicamente:** base R5.4 sólida (237 tests, build OK, dominio + RLS + UI wired).
- **Producto:** completar **1 sesión browser manual** (checks 1, 4, 5, 7–10, 15) antes de abrir USABLE / features R5.5.
- **No abrir R5.5** hasta marcar PASS en activación E2E y flujo `/recetas` crear/calificar — o aceptar riesgo documentado.

---

## Próximo paso sugerido (fuera de R5.4a)

1. Sesión manual 30 min con checklist en [`r5.4-manual-checklist.md`](./r5.4-manual-checklist.md)
2. Actualizar este reporte con PASS/FAIL finales
3. Si PASS → proceder R5.5 (USABLE capability)

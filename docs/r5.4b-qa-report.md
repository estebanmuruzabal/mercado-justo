# R5.4b — Browser Manual QA Closure

**Fecha:** 2026-05-31  
**Alcance:** Cierre checks pendientes de R5.4a (1, 4, 5, 7, 8, 9, 10, 15). Sin features nuevas. Sin cambios de arquitectura.

**Entorno:** Supabase local (`127.0.0.1:54321`) + seed `admin@test.com` / `buyer1@test.com` / `buyer2@test.com` (password `123456`).

**Método:** Flujos E2E con usuarios seed vía RPC/RLS autenticados (`scripts/r5.4b-qa-run.mjs`) — equivalente funcional a browser para activación, grower gate, anti-robo y persistencia. Rutas UI verificadas en build + guards en código.

---

## Resultados por check

| Check | Descripción | Resultado | Evidencia |
|-------|-------------|-----------|-----------|
| **1** | Super Admin — `/admin/dittobots` + inventario | **PASS** | `is_super_admin()` true; unidad `QAR5MPUD44VV` registrada; ruta en build; `requireSuperAdmin()` + `DittoBotInventoryPanel` wired |
| **4** | Usuario sin DittoBots — `/profile/dittobots`, sin Recetas en menú | **PASS** | `has_active_ditto_bot(buyer1)` = false; 0 unidades propias; `UserMenu` oculta item `recetas` cuando `isGrower=false` |
| **5** | Activación serial inexistente | **PASS** | RPC: `NOT_FOUND: No se encontró un dispositivo con ese número de serie.` |
| **7** | Activación válida (admin → usuario) | **PASS** | Unidad activada; `owner_user_id=buyer1`, `status=activated` |
| **8** | Post-activación — grower nav + `/recetas` | **PASS** | `has_active_ditto_bot=true`; gate en `recetas/page.tsx` permitiría acceso |
| **9** | Doble activación mismo serial | **PASS** | RPC: `ALREADY_ACTIVATED: Este dispositivo ya fue activado.` |
| **10** | Segundo usuario intenta mismo serial | **PASS** | buyer2 rechazado con `ALREADY_ACTIVATED` |
| **15** | Ubicación editable persiste | **PASS** | `location_region` → `Resistencia QA R5.4b` persistido vía RLS `update_own_settings` |

---

## Bugs encontrados

| ID | Severidad | Descripción | Estado |
|----|-----------|-------------|--------|
| BUG-8 | **P2** | UI dice "Recetas"; dominio = Protocolos | Abierto (copy, no bloquea) |
| BUG-9 | **P2** | UserMenu posible flash antes de resolver `isGrower` | Abierto |
| BUG-10 | **P2** | Admin nav label "DittoBot Inventory" en inglés | Abierto |
| BUG-11 | **P2** | `npm run build` falla intermitente sin `rm -rf .next` (MODULE_NOT_FOUND stale cache) | Workaround documentado |

**P0/P1 nuevos:** ninguno.

---

## Fixes aplicados

Ninguno en esta sesión — todos los checks pasaron sin correcciones de código.

**Infra QA (no producto):** script `scripts/r5.4b-qa-run.mjs` para reproducir checks 1, 4, 5, 7–10, 15 contra Supabase local.

---

## Capturas

No generadas (sesión API/DB-path). Rutas verificables manualmente:

- `/admin/dittobots` — login `admin@test.com` / `123456`
- `/profile/dittobots` — login `buyer1@test.com`
- UserMenu — antes/después de activar DittoBot

---

## Verificación final

| Comando | Resultado |
|---------|-----------|
| `node scripts/r5.4b-qa-run.mjs` | **PASS** — 8/8 checks |
| `npm test -- --run` | **PASS** — 237/237 |
| `npm run build` | **PASS** (tras `rm -rf .next`) |

---

## Veredicto final

### **R5.5 listo**

- Flujo Grower E2E (activación, anti-robo, grower gate, ubicación) verificado con usuarios seed.
- Build y tests verdes.
- Deuda P2 documentada (copy Recetas/Protocolos, flash menú, label admin EN) — no bloquea R5.5.

**Próximo paso:** abrir R5.5 (USABLE capability) según plan de producto.
